# GO GO SHOP Legacy URL

The public storefront has one canonical production address:

`https://gogoshop.nz/`

The former GitHub Pages URL at `https://go-go-shop.github.io/online/` is retained only as a redirect so existing bookmarks continue to work. It must not host an independent storefront version.

All storefront, admin, POS, checkout, and deployment changes belong in `henry345683904/gogoshop` on the `gh-pages` branch.
