# Legacy Redirect Repository

- This repository is not a storefront source and must remain redirect-only.
- The only production storefront is `https://gogoshop.nz/`.
- Make GO GO SHOP changes in `../gogoshop-github-deploy` on `gh-pages`.
- Do not restore the retired HTML application or publish a second storefront here.
- Preserve database and migration files unless the user explicitly asks to remove them.
